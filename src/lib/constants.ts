export const PUBLIC_ROUTES: string[] = ['/'] as const;
export const AUTH_ROUTES: string[] = ['/sign-in'] as const;
export const PROTECTED_ROUTE: string = '/~' as const;
export const DEFAULT_LOGIN_REDIRECT: string = '/~' as const;
export const DEFAULT_AUTH_REDIRECT: string = '/sign-in' as const;
